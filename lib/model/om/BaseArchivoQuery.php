<?php


/**
 * Base class that represents a query for the 'archivo' table.
 *
 *
 *
 * This class was autogenerated by Propel 1.6.7 on:
 *
 * Mon Jan 23 21:33:45 2017
 *
 * @method ArchivoQuery orderById($order = Criteria::ASC) Order by the id column
 * @method ArchivoQuery orderByNombreArchivoOriginal($order = Criteria::ASC) Order by the nombre_archivo_original column
 * @method ArchivoQuery orderByNombreArchivoActual($order = Criteria::ASC) Order by the nombre_archivo_actual column
 * @method ArchivoQuery orderByUsuarioId($order = Criteria::ASC) Order by the usuario_id column
 * @method ArchivoQuery orderByCreatedAt($order = Criteria::ASC) Order by the created_at column
 * @method ArchivoQuery orderByUpdatedAt($order = Criteria::ASC) Order by the updated_at column
 * @method ArchivoQuery orderByCreatedBy($order = Criteria::ASC) Order by the created_by column
 * @method ArchivoQuery orderByUpdatedBy($order = Criteria::ASC) Order by the updated_by column
 * @method ArchivoQuery orderByCantidadRating($order = Criteria::ASC) Order by the cantidad_rating column
 * @method ArchivoQuery orderByRating($order = Criteria::ASC) Order by the rating column
 * @method ArchivoQuery orderByEstado($order = Criteria::ASC) Order by the estado column
 * @method ArchivoQuery orderByDescripcion($order = Criteria::ASC) Order by the descripcion column
 * @method ArchivoQuery orderByEtiqueta($order = Criteria::ASC) Order by the etiqueta column
 *
 * @method ArchivoQuery groupById() Group by the id column
 * @method ArchivoQuery groupByNombreArchivoOriginal() Group by the nombre_archivo_original column
 * @method ArchivoQuery groupByNombreArchivoActual() Group by the nombre_archivo_actual column
 * @method ArchivoQuery groupByUsuarioId() Group by the usuario_id column
 * @method ArchivoQuery groupByCreatedAt() Group by the created_at column
 * @method ArchivoQuery groupByUpdatedAt() Group by the updated_at column
 * @method ArchivoQuery groupByCreatedBy() Group by the created_by column
 * @method ArchivoQuery groupByUpdatedBy() Group by the updated_by column
 * @method ArchivoQuery groupByCantidadRating() Group by the cantidad_rating column
 * @method ArchivoQuery groupByRating() Group by the rating column
 * @method ArchivoQuery groupByEstado() Group by the estado column
 * @method ArchivoQuery groupByDescripcion() Group by the descripcion column
 * @method ArchivoQuery groupByEtiqueta() Group by the etiqueta column
 *
 * @method ArchivoQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method ArchivoQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method ArchivoQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method ArchivoQuery leftJoinUsuario($relationAlias = null) Adds a LEFT JOIN clause to the query using the Usuario relation
 * @method ArchivoQuery rightJoinUsuario($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Usuario relation
 * @method ArchivoQuery innerJoinUsuario($relationAlias = null) Adds a INNER JOIN clause to the query using the Usuario relation
 *
 * @method ArchivoQuery leftJoinArchivoComentario($relationAlias = null) Adds a LEFT JOIN clause to the query using the ArchivoComentario relation
 * @method ArchivoQuery rightJoinArchivoComentario($relationAlias = null) Adds a RIGHT JOIN clause to the query using the ArchivoComentario relation
 * @method ArchivoQuery innerJoinArchivoComentario($relationAlias = null) Adds a INNER JOIN clause to the query using the ArchivoComentario relation
 *
 * @method Archivo findOne(PropelPDO $con = null) Return the first Archivo matching the query
 * @method Archivo findOneOrCreate(PropelPDO $con = null) Return the first Archivo matching the query, or a new Archivo object populated from the query conditions when no match is found
 *
 * @method Archivo findOneById(int $id) Return the first Archivo filtered by the id column
 * @method Archivo findOneByNombreArchivoOriginal(string $nombre_archivo_original) Return the first Archivo filtered by the nombre_archivo_original column
 * @method Archivo findOneByNombreArchivoActual(string $nombre_archivo_actual) Return the first Archivo filtered by the nombre_archivo_actual column
 * @method Archivo findOneByUsuarioId(int $usuario_id) Return the first Archivo filtered by the usuario_id column
 * @method Archivo findOneByCreatedAt(string $created_at) Return the first Archivo filtered by the created_at column
 * @method Archivo findOneByUpdatedAt(string $updated_at) Return the first Archivo filtered by the updated_at column
 * @method Archivo findOneByCreatedBy(string $created_by) Return the first Archivo filtered by the created_by column
 * @method Archivo findOneByUpdatedBy(string $updated_by) Return the first Archivo filtered by the updated_by column
 * @method Archivo findOneByCantidadRating(int $cantidad_rating) Return the first Archivo filtered by the cantidad_rating column
 * @method Archivo findOneByRating(double $rating) Return the first Archivo filtered by the rating column
 * @method Archivo findOneByEstado(string $estado) Return the first Archivo filtered by the estado column
 * @method Archivo findOneByDescripcion(string $descripcion) Return the first Archivo filtered by the descripcion column
 * @method Archivo findOneByEtiqueta(string $etiqueta) Return the first Archivo filtered by the etiqueta column
 *
 * @method array findById(int $id) Return Archivo objects filtered by the id column
 * @method array findByNombreArchivoOriginal(string $nombre_archivo_original) Return Archivo objects filtered by the nombre_archivo_original column
 * @method array findByNombreArchivoActual(string $nombre_archivo_actual) Return Archivo objects filtered by the nombre_archivo_actual column
 * @method array findByUsuarioId(int $usuario_id) Return Archivo objects filtered by the usuario_id column
 * @method array findByCreatedAt(string $created_at) Return Archivo objects filtered by the created_at column
 * @method array findByUpdatedAt(string $updated_at) Return Archivo objects filtered by the updated_at column
 * @method array findByCreatedBy(string $created_by) Return Archivo objects filtered by the created_by column
 * @method array findByUpdatedBy(string $updated_by) Return Archivo objects filtered by the updated_by column
 * @method array findByCantidadRating(int $cantidad_rating) Return Archivo objects filtered by the cantidad_rating column
 * @method array findByRating(double $rating) Return Archivo objects filtered by the rating column
 * @method array findByEstado(string $estado) Return Archivo objects filtered by the estado column
 * @method array findByDescripcion(string $descripcion) Return Archivo objects filtered by the descripcion column
 * @method array findByEtiqueta(string $etiqueta) Return Archivo objects filtered by the etiqueta column
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseArchivoQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BaseArchivoQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = 'propel', $modelName = 'Archivo', $modelAlias = null)
    {
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new ArchivoQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param     ArchivoQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return ArchivoQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof ArchivoQuery) {
            return $criteria;
        }
        $query = new ArchivoQuery();
        if (null !== $modelAlias) {
            $query->setModelAlias($modelAlias);
        }
        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return   Archivo|Archivo[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = ArchivoPeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is alredy in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(ArchivoPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return   Archivo A model object, or null if the key is not found
     * @throws   PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT `ID`, `NOMBRE_ARCHIVO_ORIGINAL`, `NOMBRE_ARCHIVO_ACTUAL`, `USUARIO_ID`, `CREATED_AT`, `UPDATED_AT`, `CREATED_BY`, `UPDATED_BY`, `CANTIDAD_RATING`, `RATING`, `ESTADO`, `DESCRIPCION`, `ETIQUETA` FROM `archivo` WHERE `ID` = :p0';
        try {
            $stmt = $con->prepare($sql);
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new Archivo();
            $obj->hydrate($row);
            ArchivoPeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return Archivo|Archivo[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|Archivo[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return ArchivoQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(ArchivoPeer::ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return ArchivoQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(ArchivoPeer::ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the id column
     *
     * Example usage:
     * <code>
     * $query->filterById(1234); // WHERE id = 1234
     * $query->filterById(array(12, 34)); // WHERE id IN (12, 34)
     * $query->filterById(array('min' => 12)); // WHERE id > 12
     * </code>
     *
     * @param     mixed $id The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ArchivoQuery The current query, for fluid interface
     */
    public function filterById($id = null, $comparison = null)
    {
        if (is_array($id) && null === $comparison) {
            $comparison = Criteria::IN;
        }

        return $this->addUsingAlias(ArchivoPeer::ID, $id, $comparison);
    }

    /**
     * Filter the query on the nombre_archivo_original column
     *
     * Example usage:
     * <code>
     * $query->filterByNombreArchivoOriginal('fooValue');   // WHERE nombre_archivo_original = 'fooValue'
     * $query->filterByNombreArchivoOriginal('%fooValue%'); // WHERE nombre_archivo_original LIKE '%fooValue%'
     * </code>
     *
     * @param     string $nombreArchivoOriginal The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ArchivoQuery The current query, for fluid interface
     */
    public function filterByNombreArchivoOriginal($nombreArchivoOriginal = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($nombreArchivoOriginal)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $nombreArchivoOriginal)) {
                $nombreArchivoOriginal = str_replace('*', '%', $nombreArchivoOriginal);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ArchivoPeer::NOMBRE_ARCHIVO_ORIGINAL, $nombreArchivoOriginal, $comparison);
    }

    /**
     * Filter the query on the nombre_archivo_actual column
     *
     * Example usage:
     * <code>
     * $query->filterByNombreArchivoActual('fooValue');   // WHERE nombre_archivo_actual = 'fooValue'
     * $query->filterByNombreArchivoActual('%fooValue%'); // WHERE nombre_archivo_actual LIKE '%fooValue%'
     * </code>
     *
     * @param     string $nombreArchivoActual The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ArchivoQuery The current query, for fluid interface
     */
    public function filterByNombreArchivoActual($nombreArchivoActual = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($nombreArchivoActual)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $nombreArchivoActual)) {
                $nombreArchivoActual = str_replace('*', '%', $nombreArchivoActual);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ArchivoPeer::NOMBRE_ARCHIVO_ACTUAL, $nombreArchivoActual, $comparison);
    }

    /**
     * Filter the query on the usuario_id column
     *
     * Example usage:
     * <code>
     * $query->filterByUsuarioId(1234); // WHERE usuario_id = 1234
     * $query->filterByUsuarioId(array(12, 34)); // WHERE usuario_id IN (12, 34)
     * $query->filterByUsuarioId(array('min' => 12)); // WHERE usuario_id > 12
     * </code>
     *
     * @see       filterByUsuario()
     *
     * @param     mixed $usuarioId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ArchivoQuery The current query, for fluid interface
     */
    public function filterByUsuarioId($usuarioId = null, $comparison = null)
    {
        if (is_array($usuarioId)) {
            $useMinMax = false;
            if (isset($usuarioId['min'])) {
                $this->addUsingAlias(ArchivoPeer::USUARIO_ID, $usuarioId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($usuarioId['max'])) {
                $this->addUsingAlias(ArchivoPeer::USUARIO_ID, $usuarioId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ArchivoPeer::USUARIO_ID, $usuarioId, $comparison);
    }

    /**
     * Filter the query on the created_at column
     *
     * Example usage:
     * <code>
     * $query->filterByCreatedAt('2011-03-14'); // WHERE created_at = '2011-03-14'
     * $query->filterByCreatedAt('now'); // WHERE created_at = '2011-03-14'
     * $query->filterByCreatedAt(array('max' => 'yesterday')); // WHERE created_at > '2011-03-13'
     * </code>
     *
     * @param     mixed $createdAt The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ArchivoQuery The current query, for fluid interface
     */
    public function filterByCreatedAt($createdAt = null, $comparison = null)
    {
        if (is_array($createdAt)) {
            $useMinMax = false;
            if (isset($createdAt['min'])) {
                $this->addUsingAlias(ArchivoPeer::CREATED_AT, $createdAt['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($createdAt['max'])) {
                $this->addUsingAlias(ArchivoPeer::CREATED_AT, $createdAt['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ArchivoPeer::CREATED_AT, $createdAt, $comparison);
    }

    /**
     * Filter the query on the updated_at column
     *
     * Example usage:
     * <code>
     * $query->filterByUpdatedAt('2011-03-14'); // WHERE updated_at = '2011-03-14'
     * $query->filterByUpdatedAt('now'); // WHERE updated_at = '2011-03-14'
     * $query->filterByUpdatedAt(array('max' => 'yesterday')); // WHERE updated_at > '2011-03-13'
     * </code>
     *
     * @param     mixed $updatedAt The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ArchivoQuery The current query, for fluid interface
     */
    public function filterByUpdatedAt($updatedAt = null, $comparison = null)
    {
        if (is_array($updatedAt)) {
            $useMinMax = false;
            if (isset($updatedAt['min'])) {
                $this->addUsingAlias(ArchivoPeer::UPDATED_AT, $updatedAt['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($updatedAt['max'])) {
                $this->addUsingAlias(ArchivoPeer::UPDATED_AT, $updatedAt['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ArchivoPeer::UPDATED_AT, $updatedAt, $comparison);
    }

    /**
     * Filter the query on the created_by column
     *
     * Example usage:
     * <code>
     * $query->filterByCreatedBy('fooValue');   // WHERE created_by = 'fooValue'
     * $query->filterByCreatedBy('%fooValue%'); // WHERE created_by LIKE '%fooValue%'
     * </code>
     *
     * @param     string $createdBy The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ArchivoQuery The current query, for fluid interface
     */
    public function filterByCreatedBy($createdBy = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($createdBy)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $createdBy)) {
                $createdBy = str_replace('*', '%', $createdBy);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ArchivoPeer::CREATED_BY, $createdBy, $comparison);
    }

    /**
     * Filter the query on the updated_by column
     *
     * Example usage:
     * <code>
     * $query->filterByUpdatedBy('fooValue');   // WHERE updated_by = 'fooValue'
     * $query->filterByUpdatedBy('%fooValue%'); // WHERE updated_by LIKE '%fooValue%'
     * </code>
     *
     * @param     string $updatedBy The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ArchivoQuery The current query, for fluid interface
     */
    public function filterByUpdatedBy($updatedBy = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($updatedBy)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $updatedBy)) {
                $updatedBy = str_replace('*', '%', $updatedBy);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ArchivoPeer::UPDATED_BY, $updatedBy, $comparison);
    }

    /**
     * Filter the query on the cantidad_rating column
     *
     * Example usage:
     * <code>
     * $query->filterByCantidadRating(1234); // WHERE cantidad_rating = 1234
     * $query->filterByCantidadRating(array(12, 34)); // WHERE cantidad_rating IN (12, 34)
     * $query->filterByCantidadRating(array('min' => 12)); // WHERE cantidad_rating > 12
     * </code>
     *
     * @param     mixed $cantidadRating The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ArchivoQuery The current query, for fluid interface
     */
    public function filterByCantidadRating($cantidadRating = null, $comparison = null)
    {
        if (is_array($cantidadRating)) {
            $useMinMax = false;
            if (isset($cantidadRating['min'])) {
                $this->addUsingAlias(ArchivoPeer::CANTIDAD_RATING, $cantidadRating['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($cantidadRating['max'])) {
                $this->addUsingAlias(ArchivoPeer::CANTIDAD_RATING, $cantidadRating['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ArchivoPeer::CANTIDAD_RATING, $cantidadRating, $comparison);
    }

    /**
     * Filter the query on the rating column
     *
     * Example usage:
     * <code>
     * $query->filterByRating(1234); // WHERE rating = 1234
     * $query->filterByRating(array(12, 34)); // WHERE rating IN (12, 34)
     * $query->filterByRating(array('min' => 12)); // WHERE rating > 12
     * </code>
     *
     * @param     mixed $rating The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ArchivoQuery The current query, for fluid interface
     */
    public function filterByRating($rating = null, $comparison = null)
    {
        if (is_array($rating)) {
            $useMinMax = false;
            if (isset($rating['min'])) {
                $this->addUsingAlias(ArchivoPeer::RATING, $rating['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($rating['max'])) {
                $this->addUsingAlias(ArchivoPeer::RATING, $rating['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ArchivoPeer::RATING, $rating, $comparison);
    }

    /**
     * Filter the query on the estado column
     *
     * Example usage:
     * <code>
     * $query->filterByEstado('fooValue');   // WHERE estado = 'fooValue'
     * $query->filterByEstado('%fooValue%'); // WHERE estado LIKE '%fooValue%'
     * </code>
     *
     * @param     string $estado The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ArchivoQuery The current query, for fluid interface
     */
    public function filterByEstado($estado = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($estado)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $estado)) {
                $estado = str_replace('*', '%', $estado);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ArchivoPeer::ESTADO, $estado, $comparison);
    }

    /**
     * Filter the query on the descripcion column
     *
     * Example usage:
     * <code>
     * $query->filterByDescripcion('fooValue');   // WHERE descripcion = 'fooValue'
     * $query->filterByDescripcion('%fooValue%'); // WHERE descripcion LIKE '%fooValue%'
     * </code>
     *
     * @param     string $descripcion The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ArchivoQuery The current query, for fluid interface
     */
    public function filterByDescripcion($descripcion = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($descripcion)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $descripcion)) {
                $descripcion = str_replace('*', '%', $descripcion);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ArchivoPeer::DESCRIPCION, $descripcion, $comparison);
    }

    /**
     * Filter the query on the etiqueta column
     *
     * Example usage:
     * <code>
     * $query->filterByEtiqueta('fooValue');   // WHERE etiqueta = 'fooValue'
     * $query->filterByEtiqueta('%fooValue%'); // WHERE etiqueta LIKE '%fooValue%'
     * </code>
     *
     * @param     string $etiqueta The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ArchivoQuery The current query, for fluid interface
     */
    public function filterByEtiqueta($etiqueta = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($etiqueta)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $etiqueta)) {
                $etiqueta = str_replace('*', '%', $etiqueta);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ArchivoPeer::ETIQUETA, $etiqueta, $comparison);
    }

    /**
     * Filter the query by a related Usuario object
     *
     * @param   Usuario|PropelObjectCollection $usuario The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   ArchivoQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByUsuario($usuario, $comparison = null)
    {
        if ($usuario instanceof Usuario) {
            return $this
                ->addUsingAlias(ArchivoPeer::USUARIO_ID, $usuario->getId(), $comparison);
        } elseif ($usuario instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(ArchivoPeer::USUARIO_ID, $usuario->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByUsuario() only accepts arguments of type Usuario or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Usuario relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ArchivoQuery The current query, for fluid interface
     */
    public function joinUsuario($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Usuario');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Usuario');
        }

        return $this;
    }

    /**
     * Use the Usuario relation Usuario object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   UsuarioQuery A secondary query class using the current class as primary query
     */
    public function useUsuarioQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinUsuario($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Usuario', 'UsuarioQuery');
    }

    /**
     * Filter the query by a related ArchivoComentario object
     *
     * @param   ArchivoComentario|PropelObjectCollection $archivoComentario  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   ArchivoQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByArchivoComentario($archivoComentario, $comparison = null)
    {
        if ($archivoComentario instanceof ArchivoComentario) {
            return $this
                ->addUsingAlias(ArchivoPeer::ID, $archivoComentario->getArchivoId(), $comparison);
        } elseif ($archivoComentario instanceof PropelObjectCollection) {
            return $this
                ->useArchivoComentarioQuery()
                ->filterByPrimaryKeys($archivoComentario->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByArchivoComentario() only accepts arguments of type ArchivoComentario or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the ArchivoComentario relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ArchivoQuery The current query, for fluid interface
     */
    public function joinArchivoComentario($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('ArchivoComentario');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'ArchivoComentario');
        }

        return $this;
    }

    /**
     * Use the ArchivoComentario relation ArchivoComentario object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ArchivoComentarioQuery A secondary query class using the current class as primary query
     */
    public function useArchivoComentarioQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinArchivoComentario($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'ArchivoComentario', 'ArchivoComentarioQuery');
    }

    /**
     * Exclude object from result
     *
     * @param   Archivo $archivo Object to remove from the list of results
     *
     * @return ArchivoQuery The current query, for fluid interface
     */
    public function prune($archivo = null)
    {
        if ($archivo) {
            $this->addUsingAlias(ArchivoPeer::ID, $archivo->getId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}
